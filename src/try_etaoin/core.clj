(ns try-etaoin.core
  (:gen-class)
  (:require [etaoin.keys :as k]
            [etaoin.api :as eta]
            [etaoin.dev :as dev]
            [try-etaoin.main :as main]))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
